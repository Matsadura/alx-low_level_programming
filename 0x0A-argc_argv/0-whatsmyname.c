#include <stdio.h>

/**
 * main - prints the program's name
 * @argc: Argument counts
 * @argv: Arguments
 * Return: First string
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
