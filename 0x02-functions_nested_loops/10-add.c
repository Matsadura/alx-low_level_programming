#include "main.h"

/**
 * add - adds two integers and returns the result
 * @x: first integer
 * @y: second integer
 * Return: the result
 */

int add(int x, int y)
{
	int z;

	z = x + y;

	return (z);
}
